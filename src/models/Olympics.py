class Olympics:
        def __init__(self):
            self.countries = [];
            self.divisions = [];
            self.results = []

        def add_country(self, country):
            self.countries.append(country);

        def add_division(self, division):
            self.countries.append(division);

        def add_result(self, result):
            self.results.append(result)
